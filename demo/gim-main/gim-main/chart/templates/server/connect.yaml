# deployment 配置
apiVersion: apps/v1
kind: Deployment
metadata:
  name: connect-deployment
  namespace: {{ $.Values.namespace }}
  labels:
    app: {{ .Values.client.connect.name }}
spec:
  replicas: {{ .Values.client.connect.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.client.connect.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.client.connect.name }}
    spec:
      containers:
        - name: {{ .Values.client.connect.name }}
          image: {{ .Values.client.connect.image }}
          imagePullPolicy: IfNotPresent     # 在kind中需要指定，不然会强制到远程拉取镜像，导致部署失败
          ports:
            - containerPort: 8000
            - containerPort: 8001
            - containerPort: 8002
          volumeMounts: # 映射文件为宿主机文件
            - mountPath: /log/
              name: log
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: ENV
              value: {{ $.Values.env }}
      volumes:
        - name: log
          hostPath:
            path: /log/
---
# service 配置
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.client.connect.name }}
  namespace: {{ $.Values.namespace }}
  labels:
    app: {{ .Values.client.connect.name }}  # 只有设置label,才能被服务发现找到
spec:
  selector:
    app: {{ .Values.client.connect.name }}
  ports:
    - name: rpc
      protocol: TCP
      port: 8000
      targetPort: 8000
    - name: tcp
      protocol: TCP
      port: 8001
      targetPort: 8001
    - name: websocket
      protocol: TCP
      port: 8002
      targetPort: 8002
